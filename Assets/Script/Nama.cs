using UnityEngine;

public class Nama : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Nama Saya Adalah Faraz");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
